name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  secrets:
    name: Secrets scan
    uses: fwd-ford/.github/.github/workflows/secrets-scan.yml@main
